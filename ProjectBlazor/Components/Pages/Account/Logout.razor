@page "/account/logout"
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/do-logout", forceLoad: true);
    }
}